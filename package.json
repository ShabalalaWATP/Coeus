{
  "name": "coeus",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "dev": "pwsh ./scripts/dev.ps1",
    "line-limit": "node scripts/check-file-lines.mjs",
    "lint": "pnpm --filter @coeus/web lint",
    "test": "pnpm --filter @coeus/web test && uv run --directory apps/api pytest",
    "typecheck": "pnpm --filter @coeus/web typecheck && uv run --directory apps/api mypy src",
    "format:check": "pnpm --filter @coeus/web format:check && uv run --directory apps/api ruff format --check src tests",
    "dead-code": "knip",
    "contracts:generate": "uv run --project apps/api python scripts/generate_openapi.py",
    "contracts:check": "uv run --project apps/api python scripts/generate_openapi.py --check"
  },
  "devDependencies": {
    "knip": "^6.24.0"
  }
}
