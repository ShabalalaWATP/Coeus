# Synthetic library and search assurance delivery note

## 15 July 2026

- Added 144 deterministic four-page PDFs to the live local Store, taking a
  fresh demo to 189 products.
- Added 15 specialist country and capability ACGs, taking the demo to 58.
- Assigned Billy Gilmour to 56 ACGs, with Russia SIGINT and China cyber held
  back as deliberate deny-path controls.
- Replaced placeholder PDF bytes with parseable deterministic documents whose
  size and SHA-256 metadata match the stored object.
- Added a versioned export of all 144 synthetic PDFs plus a digest manifest for
  GitHub demonstrations, without making those files the runtime data source.
- Calibrated RFI and Store scores around absolute evidence and retained rank
  fusion only as a small ordering signal.
- Weighted title and labelled metadata, expanded defence-domain semantic labels,
  raised the bounded local semantic work set to 500 and stopped requester-scoped
  candidate counts leaking to other ticket viewers.
- Aligned PostgreSQL visibility queries and aggregates with the synthetic
  release-marker policy.

Verification: 993 backend tests passed against local PostgreSQL with 97.62 per
cent combined coverage. The frontend suite passed with 98.54 per cent line and
95.09 per cent branch coverage, and the production build succeeded. A rendered
four-page corpus sample was visually inspected with no clipping or overlap.
