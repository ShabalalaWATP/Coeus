# Historical false positive from the PR 109 squash commit. The current empty
# example assignment uses an inline annotation, so future commits are stable.
78bf90f02aa0d69f86205ab589cedbe26a96815c:.env.example:generic-api-key:40
963a69ee2d649821afd4f799b35ef3eb531b5045:.env.example:generic-api-key:40

# Synthetic-fixture idempotency keys in tests. These are replay keys, not
# credentials, and the rule matches them only because the identifier ends in
# "key". The current lines carry same-line annotations, so future commits are
# stable; these fingerprints cover the PR 158 squash commit that predates them.
# A fingerprint names the commit that introduced the line, so squashing a branch
# re-fingerprints every finding on it: entries for the pre-squash branch commits
# would never match main and are not kept.
ff0a0ebe46a92deb7977124874dc2ef71c333f5e:apps/api/tests/test_synthetic_organisation_fixture.py:generic-api-key:219
ff0a0ebe46a92deb7977124874dc2ef71c333f5e:apps/api/tests/test_synthetic_organisation_fixture.py:generic-api-key:251
